@model PersonalBookLibrary.MvcUI.Models.RoleViewModel

@{
    ViewBag.Title = "GetList";
    Layout = "~/Views/Shared/_Layout.cshtml";
    int i = 1;
}

<head>
    <style>
        .btn {
            border-radius: 5px 5px;
        }

        .label {
            font-size: 14px;
        }
    </style>
</head>

<html lang="en">
<body>

    <!-- Content -->
    <div id="content">

        <div class="container-fluid">

            <div class="row">
                <!-- Responsive table -->
                <div class="col-md-12">
                    <div class="panel panel-default">
                        <header class="panel-heading">Rol Listesi</header>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Sıra</th>
                                            <th>Adı</th>
                                            <th>Durumu</th>
                                            <th>Ekleyen Kullanıcı</th>
                                            <th>Güncelle</th>
                                            <th>Detay</th>
                                            <th>Sil</th>
                                        </tr>
                                    </thead>
                                    <tbody>

                                        @foreach (var role in Model.Roles)
                                        {

                                            <tr>
                                                <td>@i</td>
                                                <td>@role.RoleName</td>
                                                @if (role.Status)
                                                {
                                                    <td><span class="label label-success">Aktif</span></td>
                                                }
                                                else
                                                {
                                                    <td><span class="label label-danger">Pasif</span></td>
                                                }

                                                <td>@role.InsertUser</td>
                                                <td><a href="@Url.Action("Update","Role",new {role=Model.Roles.ToList(), id=role.RoleId})" class="btn btn-success" type="button">Güncelle</a></td>
                                                <td><a href="@Url.Action("Detail","Role",new {role = Model.Roles.ToList(), id=role.RoleId})" class="btn btn-info" type="button">Detay</a></td>
                                                <td><a href="@Url.Action("Delete","Role",new {role = Model.Roles.ToList(), id=role.RoleId})" class="btn btn-danger" type="button">Sil</a></td>
                                            </tr>

                                            i++;
                                        }

                                    </tbody>
                                </table>

                            </div>
                        </div>
                    </div>
                </div>
                <!-- /Responsive table -->
            </div>

        </div>
    </div>
    <!-- /Content -->

</body>
</html>
