

@model PersonalBookLibrary.MvcUI.Models.ComplexType.LentBookViewModel

@{
    ViewBag.Title = "Detail";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<head>
    <style>
        .table {
            text-align: center;
        }

        .label {
            font-size: 14px;
        }

        .btn {
            border-radius: 5px 5px;
            margin-right: 5px;
        }
    </style>
</head>

<html lang="en">
<body>

    <!-- Content -->
    <div id="content">

        <div class="container-fluid">

            <div class="row">
                <!-- Responsive table -->
                <div class="col-md-12">
                    <div class="panel panel-default">
                        <header class="panel-heading">Ödünç Kitap Detayı</header>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped">
                                    <thead>
                                        <tr>
                                            <th>Id</th>
                                            <th>Kitap Adı</th>
                                            <th>Yazar Adı</th>
                                            <th>Kategori</th>
                                            <th>Ödünç Alan Adı</th>
                                            <th>Ödünç Veren Adı</th>
                                            <th>Ödünç Tarihi</th>
                                            <th>Durumu</th>
                                            <th>Güncelleme Durumu</th>
                                            <th>Güncelleyen Kullanıcı</th>
                                            <th>Güncelleme Tarihi</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr>
                                            <td>@Model.LentBookDetail.LentBookID</td>
                                            <td>@Model.LentBookDetail.BookName</td>
                                            <td>@Model.LentBookDetail.AuthorName</td>
                                            <td>@Model.BookDetail.CategoryName</td>
                                            <td>@Model.LentBookDetail.BorrowPersonName @Model.LentBookDetail.BorrowPersonLastName</td>
                                            <td>@Model.LentBookDetail.GivenUserName</td>
                                            <td>@Model.LentBookDetail.GivenDate</td>
                                            @if (Model.LentBookDetail.Status)
                                            {
                                                <td><span class="label label-success">Aktif</span></td>
                                            }
                                            else
                                            {
                                                <td><span class="label label-danger">Pasif</span></td>
                                            }
                                            @if (Model.LentBook.LastUpdated)
                                            {
                                                <td><span class="label label-success">Evet</span></td>
                                                <td>@Model.LentBook.UpdateUser</td>
                                                <td>@Model.LentBook.UpdateDate</td>
                                            }
                                            else
                                            {
                                                <td><span class="label label-danger">Hayır</span></td>
                                                <td>Güncellenmedi</td>
                                                <td>Güncellenmedi</td>
                                            }
                                        </tr>

                                    </tbody>
                                </table>
                            </div>
                            <div>
                                <a href="@Url.Action("UndoBookList", "UndoBook")" class="btn btn-primary pull-right" type="button">Geri Dön</a>
                                <button type="button" class="btn btn-info btn-md pull-right" data-toggle="modal" data-target="#bookSummaryModalLong">Kitap Özeti</button>
                            </div>
                        </div>
                    </div>
                </div>
                <!-- /Responsive table -->
            </div>

            <!-- Modal -->
            <div class="modal fade" id="bookSummaryModalLong" tabindex="-1" role="dialog" aria-labelledby="bookSummaryModalTitle" aria-hidden="true">
                <div class="modal-dialog" role="document">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="bookSummaryModalTitle">@Model.BookDetail.BookName özeti</h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            @if (Model.BookDetail.BookSummary != null)
                            {
                                @Model.BookDetail.BookSummary
                            }
                            else
                            {
                                <p style="color:red">Kitap özeti eklenmemiştir!</p>
                            }

                        </div>
                        <div class="modal-footer">
                            <button type="button" class="btn btn-danger" data-dismiss="modal">Kapat</button>
                        </div>
                    </div>
                </div>
            </div>
            <!-- /Modal -->
        </div>
    </div>
    <!-- /Content -->

</body>
</html>
